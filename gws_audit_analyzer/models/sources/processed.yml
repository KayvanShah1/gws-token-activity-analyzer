version: 1
sources:
    - name: cleaned
      schema: "{{ target.schema }}" # processed
      # database: "{{ target.database }}"  # optional for DuckDB/MotherDuck; include if you set one
      meta:
          dagster:
              group: cleaned
      tables:
          - name: token_events
          - name: token_event_scopes
          - name: login_events
          - name: saml_events
          - name: admin_events
          - name: drive_events
